#![allow(dead_code)]

pub static  MOVE_SPEED: f32 = 0.1;
pub static  LOOK_SPEED: f32 = 0.1;

pub static PROFILER: bool = true;

use macroquad::prelude::*;
pub static  FSBLUE: Color = Color::new(0.10, 0.20, 0.30, 1.00);